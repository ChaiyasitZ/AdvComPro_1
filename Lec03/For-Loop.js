const name = ['Justin', 'Sarah', 'Christopher'];

for (let index = 0; index < name.length; index++) { // initialization, condition, update
    const element = name[index]; // body
    console.log(element); // Justin, Sarah, Christopher
}