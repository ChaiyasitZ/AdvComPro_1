const names = ['Justin', 'Sarah', 'Christopher']; // iterable

let index = 0; // initialization
while (index < names.length) { // condition
    const name = names[index]; // body
    console.log(name); // Justin, Sarah, Christopher
    index++; // update
}